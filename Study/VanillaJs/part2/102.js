const obj = {
    movie: 'Sunny',
    music: 'Like Sugar',
    style: 'Retro',
    price: Infinity
};

const arr = Object.values(obj);
console.log(arr);

/**
 * 객체에 값만 추출하기 (values)
 * 오브젝트에서 키만,벨류만 추출 하는 방법 101, 102번.
 * 네스티드 객체의 경우에는 어떻게 해야하나?
 */