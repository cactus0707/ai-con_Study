var str = 'javascript';
var num = 200;
var arr = [1, 2, 3, 4, 5];
var obj = {
        a : 1,
        b : 2,
        c : 3,
};

console.log(str);
console.log(num);
console.log(arr); // 콘솔 로그 -> 전체 출력
console.log(obj); // 콘솔 로그 -> 전체 출력..

/**
 * console <- 은 자바스크립트 내장객체. 
 * .log 메소드. // printf 처럼 간단한 에러 찾거나 그럴 용도로 쓸수 있겠다.
 * 
 * console.error // log와 동일한 로그 메세지를 출력....
 * console.debug // 에러 메세지를 출력
 * console.info // 정보성 메세지를 출력
 * console.warn //경고성 메세지를 출력
 */